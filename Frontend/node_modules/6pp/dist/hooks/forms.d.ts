import { ChangeEvent } from "react";
type FileHandlerType = "single" | "multiple";
declare const useFileHandler: <T extends FileHandlerType>(type: T, limitInMb?: number | undefined, maxFiles?: number | undefined) => {
    file: T extends "multiple" ? File[] : File | null;
    preview: T extends "multiple" ? string[] : string | null;
    error: string | null;
    changeHandler: (e: ChangeEvent<HTMLInputElement>) => void;
};
declare const useStrongPassword: () => {
    error: string;
    value: string;
    changeHandler: (e: ChangeEvent<HTMLInputElement>) => void;
};
declare const useInputValidation: <T extends string | number>(initialVal: T, validator?: (val: T) => {
    isValid: boolean;
    errorMessage: string;
} | undefined) => {
    value: T;
    changeHandler: (e: ChangeEvent<HTMLInputElement>) => void;
    error: string;
};
export { useFileHandler, useInputValidation, useStrongPassword };
