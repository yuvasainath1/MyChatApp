import * as React from "react";
import { ConfirmModalProps, UseConfirmModalProps } from "../types/hooks.types";
declare const useConfirmModal: ({ title, subtitle, confirmHandler, }: UseConfirmModalProps) => {
    ConfirmModal: ({ containerStyles, noBtnStyles, yesBtnStyles, subtitleStyles, titleStyles, containerClassName, }: ConfirmModalProps) => React.JSX.Element;
    showModal: () => void;
    close: typeof close;
};
export { useConfirmModal };
