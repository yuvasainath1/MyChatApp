import { UseRatingPropsType } from "../types/hooks.types";
import * as React from "react";
export declare const useRating: ({ IconFilled, IconOutline, maxRating, value, selectable, styles, }: UseRatingPropsType) => {
    Ratings: () => React.JSX.Element;
    rating: number;
    setRating: React.Dispatch<React.SetStateAction<number>>;
};
